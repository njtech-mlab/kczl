package cn.edu.njut.action;

import java.util.List;

import cn.edu.njut.common.dao.Dao;
import cn.edu.njut.entity.CourseAdvice;
import cn.edu.njut.entity.EvlDaily;
import cn.edu.njut.entity.Spit;

public class Config2 {
	public static void main(String[] args) {
		/*
		Dao dao = new Dao();
		List<EvlDaily> eds = (List<EvlDaily>) dao.getByHql("FROM EvlDaily");
		for(int i = 0; i < eds.size(); i++) {
			EvlDaily ed = eds.get(i);
			Spit s = new Spit(ed.getSchoolnumber(), ed.getCtid(), ed.getAdvice(), ed.getDate());
			dao.save(s);
		}
		*/
	}
}
